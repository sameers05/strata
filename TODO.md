# Strata — Open Items

## Design & Styling
- **Status**: Deferred. Minimal functional CSS added per-slice for usability only (not real design work).
- **Post-slice-3 (after Project, Group-task, and Task are all complete)**: dedicated design/styling pass across the full hierarchy. Revisit once all three levels exist and can be styled consistently together, rather than styling each slice in isolation.